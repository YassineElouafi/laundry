// Re-exported from the shared package so the API and dashboard share one
// canonical definition. See packages/shared/src/order.ts.
export { OrderStatusEnum, ORDER_TRANSITIONS } from '@laundry/shared'
